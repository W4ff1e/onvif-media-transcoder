# MediaMTX minimal configuration for ONVIF Media Transcoder

# Logging
logLevel: info
logDestinations: [stdout]

# RTSP server configuration
rtspAddress: :RTSP_PORT_PLACEHOLDER

# Stream path configuration
paths:
  STREAM_PATH_PLACEHOLDER:
    source: publisher
